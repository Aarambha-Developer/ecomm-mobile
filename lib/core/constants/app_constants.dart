class AppConstants {
  AppConstants._();

  static const String appName = 'Lumora Nine';
  static const int pageSize = 10;
  static const Duration searchDebounce = Duration(milliseconds: 400);
  static const Duration cacheTtl = Duration(minutes: 5);
}
